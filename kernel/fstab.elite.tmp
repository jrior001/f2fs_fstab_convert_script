# Android fstab file.
#<src>	<mnt_point>	<type>	<mnt_flags>	<fs_mgr_flags>
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK

# F2FSDATuserdata	/data		f2fs	noatime,nosuid,nodev,discard,nodiratime,inline_xattr,inline_data	wait,nonremovable,encryptable=extra
# EXT4DATuserdata	/data		ext4	noatime,nosuid,nodev,noauto_da_alloc,discard		wait,encryptable=extra

